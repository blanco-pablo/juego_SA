version: "3.8"
   
services:
  juego_nodejs:
    build: 
      context: .
      dockerfile: Dockerfile
    expose:
      - "80"
    environment: 
      - PUBLIC_JWT=AQUIDEBEIRLALLAVEPUBLICA
    command: npm start

