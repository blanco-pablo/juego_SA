version: "2"
   
services:
  juego_nodejs:
    build: 
      context: .
      dockerfile: Dockerfile
    expose:
      - "80"
    environment: 
      - PUBLIC_JWT=AQUIDEBEIRLALLAVEPUBLICA
    command: npm start

